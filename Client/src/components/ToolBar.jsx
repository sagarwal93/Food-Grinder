import React from 'react';

const ToolBar = React.createClass({
  render () {
    return (
      <header>
        <button>Menu</button>
        <div>Chewsier</div>
        <div>Profile</div>
      </header>

    );
  }
});

export default ToolBar;
